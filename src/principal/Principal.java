package principal;

import controlador.Controlador;

/**
 * Clase principal que inicia la aplicación del juego de Blackjack.
 */
public class Principal {

    /**
     * Método principal que ejecuta la aplicación.
     *
     * @param args Argumentos de línea de comandos (no se utilizan en esta aplicación).
     */
    public static void main(String[] args) {
        // Crear una instancia del controlador para iniciar la aplicación
        Controlador c = new Controlador();
    }
}
